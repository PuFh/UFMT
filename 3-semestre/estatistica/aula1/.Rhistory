18+20
print("Olá Mundo")
detach("package:datasets", unload = TRUE)
library(datasets, lib.loc = "C:/Program Files/R/R-4.6.0/library")
detach("package:datasets", unload = TRUE)
library(datasets, lib.loc = "C:/Program Files/R/R-4.6.0/library")
clear
clear
..
print()
print("")
clear
x <- 18
#copiar o arquivo milsa para este arqivo para trabalharmos em cima dele
milsa <- read.table("milsa.txt",head = T)#Comando head define q a 1 liha é um cabechalo
#copiar o arquivo milsa para este arqivo para trabalharmos em cima dele
milsa <- read.table("milsa.txt",head = T)#Comando head define q a 1 liha é um cabechalo
setwd("~/UFMT/3-semestre/estatistica")
#copiar o arquivo milsa para este arqivo para trabalharmos em cima dele
milsa <- read.table("milsa.txt",head = T)#Comando head define q a 1 liha é um cabechalo
setwd("~/UFMT/3-semestre/estatistica/aula1")
#copiar o arquivo milsa para este arqivo para trabalharmos em cima dele
milsa <- read.table("milsa.txt",head = T)#Comando head define q a 1 liha é um cabechalo
View(milsa)
View(milsa)
milsa
milsa
cleal
milsa
milsa
milsa
milsa
#copiar o arquivo milsa para este arqivo para trabalharmos em cima dele
milsa <- read.table("milsa.txt",head = T)#Comando head define q a 1 liha é um cabechalo
#copiar o arquivo milsa para este arqivo para trabalharmos em cima dele
milsa <- read.table("milsa.txt",head = T)#Comando head define q a 1 liha é um cabechalo
#copiar o arquivo milsa para este arqivo para trabalharmos em cima dele
milsa <- read.table("milsa.txt",head = T)#Comando head define q a 1 liha é um cabechalo
milsa
head(milsa)
head(milsa)
milsa$salario
milsa $instrucao <- factor(milsa$civil, label = c("solteiro", "casado"), levels = 1:2)
milsa$civil
milsa$instrucao <- factor(milsa$instrucao, label = c("1 grau","2 grau","superiro"),levels=1:3)
View(milsa)
milsa$instrucao <- factor(milsa$instrucao, label = c("1 grau","2 grau","superiro"),levels=1:3)
View(milsa)
milsa $civil <- factor(milsa$civil, label = c("solteiro", "casado"), levels = 1:2)
milsa$instrucao <- factor(milsa$instrucao, label = c("1 grau","2 grau","superiro"),levels=1:3, ord = T)
milsa$instrucao <- factor(milsa$instrucao, label = c("1 grau","2 grau","superiro"),lev=1:3, ord = T)
milsa$instrucao <- factor(milsa$instrucao, label = c("1 grau","2 grau","superiro"),lev=1:3, ord = T)
View(milsa)
View(milsa)
View(milsa)
#copiar o arquivo milsa para este arqivo para trabalharmos em cima dele
milsa <- read.table("milsa.txt",head = T)#Comando head define q a 1 liha é um cabechalo
View(milsa)
#faço com que o valor 1 = solteiro desta coluna, e o valor 2 = casado
milsa $civil <- factor(milsa$civil, label = c("solteiro", "casado"), levels = 1:2)
milsa$instrucao <- factor(milsa$instrucao, label = c("1 grau","2 grau","superiro"),lev=1:3, ord = T)
milsa$regiao <- factor(milsa$regiao, label = c("Captial","Interior","Outro"), levels = 1:3)
#mesma coisa
milsa$regiao <- factor(milsa$regiao, label = c("Captial","Interior","Outro"), levels = c(2,1,3))
#mesma coisa
milsa$regiao <- factor(milsa$regiao, label = c("Captial","Interior","Outro"), lev = c(2,1,3))
table(milsa$regiao)
unique(milsa$regiao)
#mesma coisa
milsa$regiao <- factor(milsa$regiao, labels = c("Captial","Interior","Outro"), lev = c(2,1,3))
View(milsa)
View(milsa)
View(milsa)
#copiar o arquivo milsa para este arqivo para trabalharmos em cima dele
milsa <- read.table("milsa.txt",head = T)#Comando head define q a 1 liha é um cabechalo
head(milsa)
#faço com que o valor 1 = solteiro desta coluna, e o valor 2 = casado
milsa $civil <- factor(milsa$civil, label = c("solteiro", "casado"), levels = 1:2)
#mesma coisa
milsa$instrucao <- factor(milsa$instrucao, label = c("1 grau","2 grau","superiro"),lev=1:3, ord = T)
#mesma coisa
milsa$regiao <- factor(milsa$regiao, labels = c("Captial","Interior","Outro"), lev = c(2,1,3))
View(milsa)
is.factor(milsa$instrucao)
is.factor(milsa$regia)
is.factor(milsa$regiao)
